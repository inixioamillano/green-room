import { IndextopointsPipe } from './indextopoints.pipe';

describe('IndextopointsPipe', () => {
  it('create an instance', () => {
    const pipe = new IndextopointsPipe();
    expect(pipe).toBeTruthy();
  });
});
